services:
  kanlinji:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
